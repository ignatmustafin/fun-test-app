import { editOne } from "./edit-one.contacts.action";
import { getOne } from "./get-one.contacts.action";
import { deleteOne } from "./delete-one.contacts.action";

export const contactsActions = {
  getOne,
  editOne,
  deleteOne,
};
